{
  "name": "mapjump",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i src/css/tailwind.css -o public/app.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i src/css/tailwind.css -o public/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.19"
  }
}
